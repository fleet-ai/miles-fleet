[pytest]
markers =
    network: downloads models/tokenizers from HF (deselected by default)
    docker: drives real Fleet env containers via local docker (deselected by default)
addopts = -m "not network and not docker"
